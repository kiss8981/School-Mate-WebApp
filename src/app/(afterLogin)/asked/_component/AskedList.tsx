import { AskedDetailWithUser } from "@/types/asked";

const AskedList = ({}: { askeds: AskedDetailWithUser[] }) => {
  return <></>;
};

const AskedCard = ({ asked }: { asked: AskedDetailWithUser }) => {
  return <></>;
};

export default AskedList;
